{
  "name": "starter-cloudflare-worker",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy --minify",
    "cf-typegen": "wrangler types --env-interface CloudflareBindings",
    "lint": "eslint",
    "lint:fix": "eslint --fix"
  },
  "dependencies": {
    "hono": "^4.7.7"
  },
  "devDependencies": {
    "wrangler": "^4.4.0"
  }
}